{
  "name": "proxy",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "npm run build && vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.9"
  },
  "dependencies": {
    "canvas": "^2.9.1",
    "face-api.js": "^0.22.2",
    "firebase": "^9.8.1"
  }
}
